[project]
name = "plastron-client"
version = "4.1.3"
requires-python = ">= 3.8"
dependencies = [
    "rdflib",
    "requests",
    "requests-jwtauth",
    "urlobject",
]

[project.optional-dependencies]
test = [
    "freezegun",
    "pytest",
    "pytest-cov",
]
